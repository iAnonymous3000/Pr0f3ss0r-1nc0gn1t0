User-agent: *
{{- if hugo.IsProduction | or (eq site.Params.env "production") }}
Disallow:
{{- else }}
Disallow: /
{{- end }}
Sitemap: {{ printf "%ssitemap.xml" (printf "%s/" (strings.TrimRight "/" (site.Params.canonicalBaseURL | default site.BaseURL))) }}
