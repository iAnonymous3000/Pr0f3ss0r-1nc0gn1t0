{{- /* Keep sitemap discovery without exposing content modification times. */ -}}
{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- range where .Pages "Sitemap.Disable" "ne" true }}
  {{- if and .Permalink (ne .Params.robotsNoIndex true) }}
  <url>
    <loc>{{ partial "seo/canonical.html" . }}</loc>
    {{- with .Sitemap.ChangeFreq }}<changefreq>{{ . }}</changefreq>{{ end }}
    {{- if ge .Sitemap.Priority 0.0 }}<priority>{{ .Sitemap.Priority }}</priority>{{ end }}
    {{- if .IsTranslated }}{{ range .AllTranslations }}
    <xhtml:link rel="alternate" hreflang="{{ .Language.LanguageCode | default .Language.Lang }}" href="{{ .Permalink }}" />
    {{- end }}{{ end }}
  </url>
  {{- end }}
  {{- end }}
</urlset>
